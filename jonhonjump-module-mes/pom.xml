<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>cn.jonhon.jump.boot</groupId>
        <artifactId>jonhonjump</artifactId>
        <version>${revision}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>jonhonjump-module-mes</artifactId>
    <packaging>jar</packaging>

    <name>${project.artifactId}</name>
    <description>
        mes 模块下，制造执行系统（Manufacturing Execution System）。
        例如说：基础数据、排班日历、设备管理、工具管理、生产管理、质量管理、仓库管理等等
    </description>

    <dependencies>
        <dependency>
            <groupId>cn.jonhon.jump.boot</groupId>
            <artifactId>jonhonjump-module-system</artifactId>
            <version>${revision}</version>
        </dependency>

        <!-- 业务组件 -->

        <!-- Web 相关 -->
        <dependency>
            <groupId>cn.jonhon.jump.boot</groupId>
            <artifactId>jonhonjump-spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>cn.jonhon.jump.boot</groupId>
            <artifactId>jonhonjump-spring-boot-starter-security</artifactId>
        </dependency>

        <!-- DB 相关 -->
        <dependency>
            <groupId>cn.jonhon.jump.boot</groupId>
            <artifactId>jonhonjump-spring-boot-starter-mybatis</artifactId>
        </dependency>

        <dependency>
            <groupId>com.oracle.database.jdbc</groupId>
            <artifactId>ojdbc8</artifactId>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>cn.jonhon.jump.boot</groupId>
            <artifactId>jonhonjump-spring-boot-starter-redis</artifactId>
        </dependency>

        <!-- 工具类相关 -->
        <dependency>
            <groupId>cn.jonhon.jump.boot</groupId>
            <artifactId>jonhonjump-spring-boot-starter-excel</artifactId>
        </dependency>

        <!-- Test 测试相关 -->
        <dependency>
            <groupId>cn.jonhon.jump.boot</groupId>
            <artifactId>jonhonjump-spring-boot-starter-test</artifactId>
        </dependency>

        <dependency>
            <groupId>com.jonhon</groupId>
            <artifactId>api-module-route</artifactId>
            <version>1.0.0</version>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <!-- 旧 MES 源码已移除，仅编译仍有对应生产代码的 process 测试。 -->
                    <testIncludes>
                        <testInclude>**/process/**/*.java</testInclude>
                    </testIncludes>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
